import React from "react";
import url from "./icon/Marked.svg";
export const Saved = ({}) => {
  return (
    <img
      width="20"
      height="20"
      src={url}
      alt="Bookmark"
      title="Bookmark"
      className="loaded"
    />
  );
};
